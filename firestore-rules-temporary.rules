rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY RULES - ALLOW ALL AUTHENTICATED USERS
    // ⚠️ USE ONLY FOR TESTING - NOT FOR PRODUCTION
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY RULES - ALLOW ALL AUTHENTICATED USERS FULL ACCESS
    // This fixes the manager permission issues
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
